import { z } from "zod";
const zDualChartType = z.object({
    primary: z["enum"]([
        'line',
        'column',
        'columnParallel',
        'area',
        'scatter'
    ]).default('column'),
    secondary: z["enum"]([
        'line',
        'column',
        'columnParallel',
        'area',
        'scatter'
    ]).default('line')
});
export { zDualChartType };

//# sourceMappingURL=dualChartType.js.map