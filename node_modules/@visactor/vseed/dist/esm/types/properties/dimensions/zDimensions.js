import { z } from "zod";
const zDimension = z.object({
    id: z.string(),
    alias: z.string().optional(),
    encoding: z["enum"]([
        'xAxis',
        'yAxis',
        'angle',
        'color',
        'detail',
        'tooltip',
        'label',
        'row',
        'column'
    ]).optional()
});
const zDimensionGroup = z.object({
    id: z.string(),
    alias: z.string().optional(),
    get children () {
        return z.array(zDimensionGroup.or(zDimension)).optional();
    }
});
const zDimensions = z.array(zDimension);
const zDimensionTree = z.array(zDimensionGroup.or(zDimension));
export { zDimension, zDimensionGroup, zDimensionTree, zDimensions };

//# sourceMappingURL=zDimensions.js.map