import { isNullish } from "remeda";
const barMaxWidth_barMaxWidth = (spec, context)=>{
    const { advancedVSeed, vseed } = context;
    const { chartType } = vseed;
    const barMaxWidth = advancedVSeed.config?.[chartType]?.barMaxWidth;
    if (!isNullish(barMaxWidth)) return {
        ...spec,
        barMaxWidth
    };
    return spec;
};
export { barMaxWidth_barMaxWidth as barMaxWidth };

//# sourceMappingURL=barMaxWidth.js.map