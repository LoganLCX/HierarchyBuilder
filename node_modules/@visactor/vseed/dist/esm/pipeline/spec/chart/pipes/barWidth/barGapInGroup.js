import { isNullish } from "remeda";
const barGapInGroup_barGapInGroup = (spec, context)=>{
    const { advancedVSeed, vseed } = context;
    const { chartType } = vseed;
    const barGapInGroup = advancedVSeed.config?.[chartType]?.barGapInGroup;
    if (!isNullish(barGapInGroup)) return {
        ...spec,
        barGapInGroup
    };
    return spec;
};
export { barGapInGroup_barGapInGroup as barGapInGroup };

//# sourceMappingURL=barGapInGroup.js.map