const roseStyle = (spec, context)=>{
    const { advancedVSeed } = context;
    const { dataset } = advancedVSeed;
    const showStroke = dataset.length <= 30;
    const result = {
        ...spec,
        rose: {
            style: {}
        }
    };
    return {
        ...result,
        rose: {
            style: {
                stroke: '#ffffff',
                lineWidth: showStroke ? 1 : 0
            },
            state: {
                hover: {
                    lineWidth: 1,
                    fillOpacity: 0.6
                }
            }
        }
    };
};
export { roseStyle };

//# sourceMappingURL=roseStyle.js.map