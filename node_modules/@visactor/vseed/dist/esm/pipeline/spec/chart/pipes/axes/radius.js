const radiusAxis = (spec)=>{
    const result = {
        ...spec
    };
    if (!result.axes) result.axes = [];
    result.axes.push({
        type: 'linear',
        orient: 'radius',
        visible: false,
        zero: true,
        nice: false
    });
    return result;
};
export { radiusAxis };

//# sourceMappingURL=radius.js.map