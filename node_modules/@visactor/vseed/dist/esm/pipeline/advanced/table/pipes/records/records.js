const records = (advancedVSeed, context)=>{
    const { vseed } = context;
    const { dataset } = vseed;
    return {
        ...advancedVSeed,
        dataset
    };
};
export { records };

//# sourceMappingURL=records.js.map