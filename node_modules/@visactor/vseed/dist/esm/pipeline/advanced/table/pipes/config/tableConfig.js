import { pick } from "remeda";
const tableConfig = (advancedVSeed, context)=>{
    const { vseed } = context;
    const { chartType } = vseed;
    const result = {
        ...advancedVSeed
    };
    const config = pick(vseed, [
        'backgroundColor',
        'bodyFontSize',
        'bodyFontColor',
        'bodyBackgroundColor',
        'headerFontSize',
        'headerFontColor',
        'headerBackgroundColor',
        'hoverHeaderBackgroundColor',
        'hoverHeaderInlineBackgroundColor',
        'selectedBorderColor',
        'selectedBackgroundColor'
    ]);
    result.config = {
        ...result.config || {},
        [chartType]: {
            ...config
        }
    };
    return result;
};
export { tableConfig };

//# sourceMappingURL=tableConfig.js.map