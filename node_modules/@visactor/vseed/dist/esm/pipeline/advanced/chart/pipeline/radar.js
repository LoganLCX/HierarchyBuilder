import { annotation, buildMeasures, defaultDimensions, defaultEncodingForRadar, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForRadar, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, radarConfig, reshapeWithEncoding, sortLegend, theme } from "../pipes/index.js";
const radarAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForRadar,
        buildMeasures
    ], [
        encodingForRadar,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    sortLegend,
    radarConfig,
    theme,
    markStyle,
    annotation
];
export { radarAdvancedPipeline };

//# sourceMappingURL=radar.js.map