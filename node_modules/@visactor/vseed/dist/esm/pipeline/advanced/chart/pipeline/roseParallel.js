import { annotation, buildMeasures, defaultDimensions, defaultEncodingForRose, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForRose, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, roseParallelConfig, theme } from "../pipes/index.js";
const roseParallelAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForRose,
        buildMeasures
    ], [
        encodingForRose,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    roseParallelConfig,
    theme,
    markStyle,
    annotation
];
export { roseParallelAdvancedPipeline };

//# sourceMappingURL=roseParallel.js.map