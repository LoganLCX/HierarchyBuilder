import { annotation, buildMeasuresForScatter, defaultDimensions, defaultEncodingForScatter, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForScatter, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, regressionLine, scatterConfig, theme } from "../pipes/index.js";
import { reshapeWithScatterEncoding } from "../pipes/reshape/reshapeWithScatterEncoding.js";
import { pivotReshapeWithScatterEncoding } from "../pipes/reshape/pivotReshapeWithScatterEncoding.js";
const scatterAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForScatter,
        buildMeasuresForScatter
    ], [
        encodingForScatter,
        buildMeasuresForScatter,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'size'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithScatterEncoding
    ], [
        pivotReshapeWithScatterEncoding
    ]),
    scatterConfig,
    theme,
    markStyle,
    annotation,
    regressionLine
];
export { scatterAdvancedPipeline };

//# sourceMappingURL=scatter.js.map