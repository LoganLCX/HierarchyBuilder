import { annotation, buildMeasures, columnParallelConfig, defaultDimensions, defaultEncodingForColumn, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForColumn, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, sortLegend, sortXBandAxis, theme } from "../pipes/index.js";
const columnParallelAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForColumn,
        buildMeasures
    ], [
        encodingForColumn,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    sortXBandAxis,
    sortLegend,
    columnParallelConfig,
    theme,
    markStyle,
    annotation
];
export { columnParallelAdvancedPipeline };

//# sourceMappingURL=columnParallel.js.map