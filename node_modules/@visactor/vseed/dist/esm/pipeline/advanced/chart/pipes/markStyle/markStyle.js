import { pick } from "remeda";
import { replaceNullToUndefined } from "../../../../utils/index.js";
const markStyle_markStyle = (advancedVSeed, context)=>{
    const { vseed } = context;
    const pickedMarkStyle = pick(vseed, [
        'barStyle',
        'pointStyle',
        'lineStyle',
        'areaStyle',
        'boxPlotStyle',
        'outlierStyle'
    ]);
    const markStyle = replaceNullToUndefined(pickedMarkStyle);
    return {
        ...advancedVSeed,
        markStyle
    };
};
export { markStyle_markStyle as markStyle };

//# sourceMappingURL=markStyle.js.map