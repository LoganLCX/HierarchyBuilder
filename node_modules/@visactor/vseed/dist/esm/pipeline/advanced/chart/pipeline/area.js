import { annotation, areaConfig, buildMeasures, defaultDimensions, defaultEncodingForLine, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForLine, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, sortLegend, sortXBandAxis, theme } from "../pipes/index.js";
const areaAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForLine,
        buildMeasures
    ], [
        encodingForLine,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    sortXBandAxis,
    sortLegend,
    areaConfig,
    theme,
    markStyle,
    annotation
];
export { areaAdvancedPipeline };

//# sourceMappingURL=area.js.map