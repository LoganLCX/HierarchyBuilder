import { annotation, buildMeasures, defaultDimensions, defaultEncodingForFunnel, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForFunnel, funnelConfig, initAdvancedVSeed, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, theme } from "../pipes/index.js";
const funnelAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForFunnel,
        buildMeasures
    ], [
        encodingForFunnel,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    funnelConfig,
    theme,
    annotation
];
export { funnelAdvancedPipeline };

//# sourceMappingURL=funnel.js.map