import { annotation, buildMeasures, defaultDimensions, defaultEncodingForRose, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForRose, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, roseConfig, theme } from "../pipes/index.js";
const roseAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForRose,
        buildMeasures
    ], [
        encodingForRose,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    roseConfig,
    theme,
    markStyle,
    annotation
];
export { roseAdvancedPipeline };

//# sourceMappingURL=rose.js.map