import { annotation, buildMeasures, defaultDimensions, defaultEncodingForHeatmap, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForHeatmap, heatmapConfig, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, sortXBandAxis, theme } from "../pipes/index.js";
const heatmapAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForHeatmap,
        buildMeasures
    ], [
        encodingForHeatmap,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    sortXBandAxis,
    heatmapConfig,
    theme,
    markStyle,
    annotation
];
export { heatmapAdvancedPipeline };

//# sourceMappingURL=heatmap.js.map