import { annotation, buildMeasures, columnPercentConfig, defaultDimensions, defaultEncodingForColumn, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForColumn, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, sortLegend, sortXBandAxis, theme } from "../pipes/index.js";
const columnPercentAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForColumn,
        buildMeasures
    ], [
        encodingForColumn,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    sortXBandAxis,
    sortLegend,
    columnPercentConfig,
    theme,
    markStyle,
    annotation
];
export { columnPercentAdvancedPipeline };

//# sourceMappingURL=columnPercent.js.map