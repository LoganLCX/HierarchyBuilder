import { annotation, buildMeasures, defaultDimensions, defaultEncodingForLine, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForLine, initAdvancedVSeed, lineConfig, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, sortLegend, sortXBandAxis, theme } from "../pipes/index.js";
const lineAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForLine,
        buildMeasures
    ], [
        encodingForLine,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    sortXBandAxis,
    sortLegend,
    lineConfig,
    theme,
    markStyle,
    annotation
];
export { lineAdvancedPipeline };

//# sourceMappingURL=line.js.map