import { annotation, buildMeasuresForDualAxis, defaultDimensions, defaultEncodingForDualAxis, defaultMeasureId, defaultMeasures, dualAxisConfig, encodingAdapter, encodingForDualAxis, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithDualEncoding, reshapeWithDualEncoding, sortLegend, sortXBandAxis, theme } from "../pipes/index.js";
const dualAxisAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForDualAxis,
        buildMeasuresForDualAxis
    ], [
        encodingForDualAxis,
        buildMeasuresForDualAxis,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithDualEncoding
    ], [
        pivotReshapeWithDualEncoding
    ]),
    sortXBandAxis,
    sortLegend,
    dualAxisConfig,
    theme,
    markStyle,
    annotation
];
export { dualAxisAdvancedPipeline };

//# sourceMappingURL=dualAxis.js.map