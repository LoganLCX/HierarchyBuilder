import { annotation, barParallelConfig, buildMeasures, defaultDimensions, defaultEncodingForBar, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForBar, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, sortLegend, sortYBandAxis, theme } from "../pipes/index.js";
const barParallelAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForBar,
        buildMeasures
    ], [
        encodingForBar,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    sortYBandAxis,
    sortLegend,
    barParallelConfig,
    theme,
    markStyle,
    annotation
];
export { barParallelAdvancedPipeline };

//# sourceMappingURL=barParallel.js.map