import { annotation, barConfig, buildMeasures, defaultDimensions, defaultEncodingForBar, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForBar, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, sortLegend, sortYBandAxis, theme } from "../pipes/index.js";
const barAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForBar,
        buildMeasures
    ], [
        encodingForBar,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    sortYBandAxis,
    sortLegend,
    barConfig,
    theme,
    markStyle,
    annotation
];
export { barAdvancedPipeline };

//# sourceMappingURL=bar.js.map