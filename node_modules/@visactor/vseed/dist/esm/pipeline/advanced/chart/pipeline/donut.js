import { annotation, buildMeasures, defaultDimensions, defaultEncodingForPie, defaultMeasureId, defaultMeasures, donutConfig, encodingAdapter, encodingForPie, initAdvancedVSeed, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, theme } from "../pipes/index.js";
const donutAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        buildMeasures,
        defaultEncodingForPie
    ], [
        buildMeasures,
        encodingForPie,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    donutConfig,
    theme,
    annotation
];
export { donutAdvancedPipeline };

//# sourceMappingURL=donut.js.map