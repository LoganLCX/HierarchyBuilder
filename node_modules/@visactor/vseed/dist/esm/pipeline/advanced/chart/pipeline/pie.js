import { annotation, buildMeasures, defaultDimensions, defaultEncodingForPie, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForPie, initAdvancedVSeed, pickDimensionsForReshape, pickMeasuresForReshape, pieConfig, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, theme } from "../pipes/index.js";
const pieAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        buildMeasures,
        defaultEncodingForPie
    ], [
        buildMeasures,
        encodingForPie,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    pieConfig,
    theme,
    annotation
];
export { pieAdvancedPipeline };

//# sourceMappingURL=pie.js.map