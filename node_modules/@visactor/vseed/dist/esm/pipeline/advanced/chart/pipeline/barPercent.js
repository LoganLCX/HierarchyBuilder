import { annotation, barPercentConfig, buildMeasures, defaultDimensions, defaultEncodingForBar, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForBar, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, reshapeWithEncoding, sortLegend, sortYBandAxis, theme } from "../pipes/index.js";
const barPercentAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForBar,
        buildMeasures
    ], [
        encodingForBar,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    sortYBandAxis,
    sortLegend,
    barPercentConfig,
    theme,
    markStyle,
    annotation
];
export { barPercentAdvancedPipeline };

//# sourceMappingURL=barPercent.js.map