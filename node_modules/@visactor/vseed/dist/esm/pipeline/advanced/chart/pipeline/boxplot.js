import { annotation, boxplotConfig, buildMeasures, defaultDimensions, defaultEncodingForBoxplot, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForBoxplot, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithBoxplotEncoding, reshapeWithBoxplotEncoding, sortLegend, theme } from "../pipes/index.js";
const boxplotAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    boxplotConfig,
    encodingAdapter([
        defaultEncodingForBoxplot,
        buildMeasures
    ], [
        encodingForBoxplot,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithBoxplotEncoding
    ], [
        pivotReshapeWithBoxplotEncoding
    ]),
    sortLegend,
    theme,
    markStyle,
    annotation
];
export { boxplotAdvancedPipeline };

//# sourceMappingURL=boxplot.js.map