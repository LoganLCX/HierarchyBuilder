import { checkVSeed } from "../../../../utils/index.js";
const initAdvancedVSeed = (advancedVSeed, context)=>{
    const { vseed } = context;
    const { chartType, locale } = vseed;
    checkVSeed(vseed);
    return {
        ...advancedVSeed,
        chartType,
        locale: locale || 'zh-CN'
    };
};
export { initAdvancedVSeed };

//# sourceMappingURL=initAdvancedVSeed.js.map