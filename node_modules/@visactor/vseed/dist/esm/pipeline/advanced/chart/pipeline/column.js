import { annotation, buildMeasures, columnConfig, defaultDimensions, defaultEncodingForColumn, defaultMeasureId, defaultMeasures, encodingAdapter, encodingForColumn, initAdvancedVSeed, markStyle, pickDimensionsForReshape, pickMeasuresForReshape, pivotAdapter, pivotReshapeWithEncoding, regressionLine, reshapeWithEncoding, sortLegend, sortXBandAxis, theme } from "../pipes/index.js";
const columnAdvancedPipeline = [
    initAdvancedVSeed,
    defaultMeasures,
    defaultDimensions,
    defaultMeasureId,
    encodingAdapter([
        defaultEncodingForColumn,
        buildMeasures
    ], [
        encodingForColumn,
        buildMeasures,
        pickMeasuresForReshape([
            'tooltip',
            'label',
            'color'
        ]),
        pickDimensionsForReshape
    ]),
    pivotAdapter([
        reshapeWithEncoding
    ], [
        pivotReshapeWithEncoding
    ]),
    sortXBandAxis,
    sortLegend,
    columnConfig,
    theme,
    markStyle,
    annotation,
    regressionLine
];
export { columnAdvancedPipeline };

//# sourceMappingURL=column.js.map