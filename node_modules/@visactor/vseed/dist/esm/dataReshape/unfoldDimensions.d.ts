import type { Dataset, Dimension, Encoding, UnfoldInfo } from '../types';
/**
 * @description 展开并合并视觉通道的维度, 在foldMeasures后合并维度, 所以不需要进行笛卡尔积
 * @param dataset 原始数据集
 * @param dimensions 维度
 * @param encoding 编码
 * @param options
 * @returns
 */
export declare const unfoldDimensions: (dataset: Dataset, dimensions: Dimension[], encoding: Encoding, options: {
    foldMeasureId: string;
    separator: string;
    colorItemAsId: boolean;
}) => {
    dataset: Dataset;
    unfoldInfo: UnfoldInfo;
};
