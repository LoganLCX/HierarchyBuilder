import type { Builder } from '../builder';
export declare const getColorItems: (builder: Builder) => {
    id: string;
    alias: string;
}[];
export declare const getColorIdMap: (builder: Builder) => Record<string, {
    id: string;
    alias: string;
}>;
