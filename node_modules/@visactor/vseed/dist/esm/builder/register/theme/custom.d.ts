import type { CustomThemeConfig } from '../../../types';
export declare const registerCustomTheme: (key: string, themeConfig: CustomThemeConfig | ((props: {
    lightTheme: CustomThemeConfig;
    darkTheme: CustomThemeConfig;
}) => CustomThemeConfig)) => void;
